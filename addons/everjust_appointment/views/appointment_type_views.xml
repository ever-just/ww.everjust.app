<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Appointment Type: Tree View -->
    <record id="appointment_type_view_tree" model="ir.ui.view">
        <field name="name">appointment.type.tree</field>
        <field name="model">appointment.type</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="duration"/>
                <field name="location"/>
                <field name="is_published"/>
                <field name="create_lead"/>
                <field name="min_schedule_hours"/>
                <field name="max_schedule_days"/>
            </tree>
        </field>
    </record>

    <!-- Appointment Type: Form View -->
    <record id="appointment_type_view_form" model="ir.ui.view">
        <field name="name">appointment.type.form</field>
        <field name="model">appointment.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <label for="name"/>
                        <h1>
                            <field name="name" placeholder="e.g. Free Consultation"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="duration"/>
                            <field name="location" placeholder="e.g. Phone, On-site, Video Call"/>
                            <field name="color" widget="color_picker"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="is_published"/>
                            <field name="create_lead"/>
                            <field name="min_schedule_hours"/>
                            <field name="max_schedule_days"/>
                        </group>
                    </group>
                    <group>
                        <field name="staff_user_ids" widget="many2many_tags"/>
                    </group>
                    <notebook>
                        <page string="Description">
                            <field name="description"/>
                        </page>
                        <page string="Available Slots">
                            <field name="slot_ids">
                                <tree editable="bottom">
                                    <field name="weekday"/>
                                    <field name="start_hour" widget="float_time"/>
                                    <field name="end_hour" widget="float_time"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Appointment Type: Search View -->
    <record id="appointment_type_view_search" model="ir.ui.view">
        <field name="name">appointment.type.search</field>
        <field name="model">appointment.type</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="location"/>
                <filter name="published" string="Published" domain="[('is_published', '=', True)]"/>
                <filter name="creates_lead" string="Creates Lead" domain="[('create_lead', '=', True)]"/>
                <group>
                    <filter name="group_location" string="Location" context="{'group_by': 'location'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Appointment Type: Action -->
    <record id="action_appointment_type" model="ir.actions.act_window">
        <field name="name">Appointment Types</field>
        <field name="res_model">appointment.type</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first appointment type
            </p>
            <p>
                Define appointment types (e.g. Free Estimate, Consultation)
                and set available time slots for each.
            </p>
        </field>
    </record>

</odoo>
