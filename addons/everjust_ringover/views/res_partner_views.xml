<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add "Call with Ringover" button to contact form -->
    <record id="view_partner_form_ringover" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.ringover</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_ringover_call"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-phone"
                        string="Call"
                        invisible="not phone and not mobile"/>
            </xpath>
        </field>
    </record>
</odoo>
